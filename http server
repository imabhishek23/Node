const http = require('http');

const PORT =  process.env.PORT || 5000

const server = http.createServer((req, res)=>{
    res.end("Hello Lond Abhishek")
})

server.listen(PORT, (req, res)=>{
    console.log(`Server is running on http://localhost:${PORT}`);
})
